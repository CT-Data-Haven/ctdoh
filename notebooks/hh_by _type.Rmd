---
title: "Households by type"
output: github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = T, message = F, warning = F)
```

```{r}
library(tidyverse)
library(tidycensus)
library(janitor)
library(cwi)
library(camiller)
```

Household type including living alone by relationship, 2012-2018 5Ys

# Fetch

```{r}
#years <- c(2012:2018)
#fetch <- years %>% map(~multi_geo_acs(table = "B09019", year = ., new_england = F))
#hhtype <- bind_rows(hhtype) %>% label_acs()
```

# Clean

# Calculate change