---
title: "Households by size"
output: github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = T, message = F, warning = F)
```

```{r}
library(tidyverse)
library(tidycensus)
library(janitor)
library(cwi)
library(camiller)
```

Use size by veh available... B08201 (2012-2018)

# Fetch

```{r}
#years <- c(2012:2018)
#fetch <- years %>% map(~multi_geo_acs(table = "B008501", year = ., new_england = F))
#hhtype <- bind_rows(hhtype) %>% label_acs()
```

# Clean

# Calculate change